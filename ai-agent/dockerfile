FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    anthropic \
    openai \
    requests \
    redis \
    psycopg2-binary \
    python-dotenv

COPY src/ /app/src/

CMD ["python", "-u", "src/agent.py"]
